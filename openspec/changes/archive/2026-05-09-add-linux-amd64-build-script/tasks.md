## 1. Build Tooling

- [x] 1.1 Add a `build.sh` script that wraps `docker buildx build`
- [x] 1.2 Default the script to `linux/amd64` and support configurable image metadata plus push/load behavior
- [x] 1.3 Make the script executable and verify its shell syntax

## 2. Documentation And Spec

- [x] 2.1 Document the build script in `README.md`
- [x] 2.2 Add an OpenSpec change and deployment tooling spec coverage for Docker build/runtime assets
- [x] 2.3 Validate the new OpenSpec change
