# Non-secret runtime settings live in config.yaml.
# Copy config.example.yaml to config.yaml before starting the service.
# CONFIG_PATH=config.yaml

SUB2API_ADMIN_API_KEY=replace-me
DEFAULT_USER_PASSWORD=ChangeMe123!

# Optional: pin the local admin password instead of generating one at startup.
# APP_AUTH_PASSWORD=optional-test-override
